const fs = require('fs')
const fsProm = require('fs').promises
const http = require('http')
const path = require('path')
const rl = require('readline')
const log = console.log


module.exports = {
    fs, fsProm, http, path, rl, log
}